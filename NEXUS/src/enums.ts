export enum Routes {}

export enum Constants {
  ERROR = "error",
}

export enum HttpMethod {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  PATCH = "PATCH",
  DELETE = "DELETE",
}

export enum ElementSend {
  EMAIL = "EMAIL",
  SMS = "SMS",
}

export enum LogType {
  LOG = "log",
  WARN = "warn",
  ERROR = "error",
}